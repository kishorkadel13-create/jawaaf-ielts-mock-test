## Plan: Create `ielts-mock` GitHub repo and push the project (safely)

**Decisions locked in:**
- Repo name: `ielts-mock` (public)
- Auth: install `gh` CLI via Homebrew, then `gh auth login`
- Git identity (local to this repo): name `Kishor Kandel`, email `kishorkadel42@gmail.com` (correct me if wrong)

### Step 1 — Protect secrets before anything else (root `.gitignore`)
Create a single comprehensive `/Users/kishorkandel/projects/IELTS MOCK/.gitignore` that excludes:
- `.env`, `**/.env`, `.env.local`, `.env.*.local` → keeps real Supabase keys / JWT secret OUT
- `node_modules/` (both backend & frontend)
- `dist/`, `dist-ssr/` (frontend build output)
- `.DS_Store` (macOS noise — there are 7 of these)
- logs, `*.log`, `npm-debug.log*`, editor dirs (`.vscode`, `.idea`), OS junk
- `backend/src/uploads/*` (keep dir via `.gitkeep`) — user uploads shouldn't be in git

Keep `.env.example` files tracked (they're safe templates, already redacted).

### Step 2 — Set local git identity
```
git config user.name "Kishor Kandel"
git config user.email "kishorkadel42@gmail.com"
```
(local to this repo only — won't touch your global config)

### Step 3 — Install & authenticate `gh` CLI
- `brew install gh` (if not already installed)
- `gh auth login` → choose **GitHub.com** → **HTTPS** → **Login with a web browser** (you'll paste a one-time code in the browser)

### Step 4 — Verify secrets are excluded BEFORE committing
Run `git status --porcelain` and `git check-ignore backend/.env frontend/.env` to confirm the real `.env` files are ignored. I will show you the exact staged file list before committing so nothing sensitive slips in.

### Step 5 — Initial commit
```
git add .
git commit -m "Initial commit: IELTS mock test platform (backend + frontend)"
```

### Step 6 — Create the GitHub repo and push
```
gh repo create ielts-mock --public --source=. --remote=origin --push --description "IELTS mock test platform"
```
This creates `ielts-mock` under your account, sets `origin`, and pushes `main` in one go.

### Step 7 — Post-push verification
- Confirm `.env` files are NOT on GitHub: `git ls-files | grep -E '\.env$'` should return nothing.
- Show you the repo URL.

### Notes / things I'll confirm with you during execution
- Name `Kishor Kandel` is assumed from your folder name — say the word if it should differ.
- `gh auth login` and `gh repo create` interact with your GitHub account (outward-facing); I'll run `gh auth login` but you'll complete the browser step yourself.
- I will NOT touch your global git config.