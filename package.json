{
  "name": "ielts-mock-local",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev:frontend",
    "build": "npm --prefix frontend run build"
  }
}
